// GhosttyKit is optional. The Build terminal currently uses the native
// Process-backed surface; re-enable `@import GhosttyKit;` when the xcframework
// is available via scripts/ensure-ghosttykit.sh.
